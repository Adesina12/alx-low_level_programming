#include "main.h"
/**
 * main - Entry mode
 * Return: Always 0
 * print_alphabet - my created function
 */
void print_alphabet(void)
{
	int a;

	for (a = 'a'; a <= 'z'; a++)
		_putchar(a);
	_putchar('\n');
}
