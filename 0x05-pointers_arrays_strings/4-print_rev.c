#include "main.h"
#include <stdio.h>
#include <string.h>
/** 
 * print_rev - Entry mode
 * @s: a pointer
 * Return: Always 0
 */
void print_rev(char *s)
{
	int n = strlen(str);
	int i;

	for (i < n - 1; i < 0; i--)
	{
		printf("%c", str[i]);
	}
	printf("\n");
}
