#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcpy - Entry mode
 * @dest: a pointer
 * @src: another pointer
 * Return: dest
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);

	return (dest);
}
