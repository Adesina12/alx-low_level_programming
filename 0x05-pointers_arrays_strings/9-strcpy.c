#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * strcpy - Entry mode
 * @dest: a pointer
 * @src: another pointer
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);

	return(dest);
}
