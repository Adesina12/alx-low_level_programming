#include "main.h"
#include <string.h>

/**
 * _strcmp - Entry mode
 * @s1: a pointer
 * @s2: another pointer
 * Return: s1
 */

int _strcmp(char *s1, char *s2)
{
	if (strcmp(s1, s2) == 0)
		return (0);
}
